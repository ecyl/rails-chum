<div class="container d-flex mx-auto">
  <div class="container bg-platinum">
    <p class="text-center mt-5 text-l" id='current-travellers-text'>Current travelers in this trip</p>

    <% if itinerary.itinerary_users.where(status:"accepted").empty? %>
      <p class='text-center mt-3 mb-5 '>There are currently no travellers on this trip. Send a request to join now!</p>
    <% else %>
      <div class='flex-container-around wrap mt-4 p-5'>
        <% accepted_users.each do |itinerary_user| %>
          <% fname = "#{itinerary_user.user.first_name} #{itinerary_user.user.last_name}" %>
          <%= link_to user_path(itinerary_user.user) do  %>
            <div class="rayan mx-5 mb-3">
              <div class='d-flex justify-content-center'>
                <%= image_tag("https://kitt.lewagon.com/placeholder/users/#{fname}", class: "avatar-xl avatar-margin-bottom") %>
              </div>
              <p class='text-center' style="text-decoration: none;"><%= fname %></p>
            </div>
          <% end %>

        <% end %>
      </div>
    <% end %>
  </div>
  </div>
